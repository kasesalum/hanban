PORT=5050
FRONTEND_URL=http://localhost:3000
# Public URL of this API, used in image links. Defaults to the incoming request host.
API_PUBLIC_URL=
# Optional local image directory. Defaults to ./uploads
UPLOAD_DIR=
# Used when Firebase Storage is enabled (usually PROJECT_ID.firebasestorage.app).
FIREBASE_STORAGE_BUCKET=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
